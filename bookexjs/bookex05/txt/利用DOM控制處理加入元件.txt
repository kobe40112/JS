利用DOM控制處理加入元件

1.建立元件
createElement() :建立一個新元件開始。這個元件節點可以儲存在一個變數中。

2.提供內容
createTextNode():會建立一個新文字節點。再一次，將這個節點儲存在一個變數中。利用appendChild()方法將它加入至元件節點。

3.加入至DOM
appendChild()
現在已經有了一個元件(和非必要的文字節點)，接著可以使用appendChild()方法允許你自行指定將元件加入至任一樹節點下，做為它的子節點。